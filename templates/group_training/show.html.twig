{% extends 'base.html.twig' %}

{% block title %}GroupTraining{% endblock %}

{% block body %}
    <h1>GroupTraining</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group_training.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ group_training.name }}</td>
            </tr>
            <tr>
                <th>Trainer</th>
                <td>{{ group_training.trainer }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ group_training.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('group_training_index') }}">back to list</a>

    <a href="{{ path('group_training_edit', {'id': group_training.id}) }}">edit</a>

    {{ include('group_training/_delete_form.html.twig') }}
{% endblock %}


